type StudySpaceInfo = {
    name: string,
    description: string,
    locationLink: string,
    rating: number,
    distance: number,
    id: number,
    hasFood: boolean
}

type chunkCoord = {
    q: number,
    r: number
}
